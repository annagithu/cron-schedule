import React from "react"

const ChangeButton = ({ onClick }) => {
    return (
        <button onClick={onClick}>
            Enter value
        </button>
    );
};

export default ChangeButton